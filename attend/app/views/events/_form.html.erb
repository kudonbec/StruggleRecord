<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :no %> : <%= f.text_field :no %><br />
    <%= f.label :name %> : <%= f.text_field :name %><br />
    <%= f.label :capacity %><%= f.text_field :capacity %><br /><br />
    <%= f.label :summary %><br /><%= f.text_area :summary %><br /><br />
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
